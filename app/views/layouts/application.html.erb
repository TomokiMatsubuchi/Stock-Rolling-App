<!DOCTYPE html>
<html>
  <head>
    <title>Stock Rolling App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div id="contents">
      <header class="flex jutsify-between">
        <div class="logo">
          <% if user_signed_in? %>
            <%= link_to image_tag('logo_transparent.png', class: "logo-width"), expendable_items_path  %>
          <% else %>
            <%= link_to image_tag('logo_transparent.png', class: "logo-width"), new_user_session_path  %>
          <% end %>
        </div>
      </header>
      <main id="main">
        <%= yield %>
        <div class="flex justify-center m-10 p-10 text-xl border-t border-b border-black">
          <p class="text-sm md:text-lg">Product By Tomoki Matsubuchi</p>
        </div>
      </main>
      <div class="hamburger">
        <span></span>
        <span></span>
        <span></span>
      </div>
      <div id="sub">
        <nav class="globalMenuSp box">
          <ul class="whitespace-nowrap">
            <% if user_signed_in? %>
              <% if user_admin? %>
                <li><%= link_to t('defaults.admin_page'), admin_users_path %></li>
              <% end %>
              <li><%= link_to t('defaults.shopping_memo'), expendable_items_path %></li>
              <li><%= link_to t('defaults.new_expendable_item'), new_expendable_item_path %></li>
              <li><%= link_to 'myaccount', user_path(current_user.id) %></li>
              <li><%= link_to t('defaults.add_line'), 'https://lin.ee/YTbl29b' %></li>
              <li><%= link_to 'Log out', destroy_user_session_path, class: 'dropdown-item', method: :delete %></li>
            <% else %>
              <li><%= link_to 'Welcome to Stock Roller', new_user_session_path, class: 'md:text-xs'%></li>
              <li><%= link_to t('defaults.add_line'), 'https://lin.ee/YTbl29b' %></li>
            <% end %>
          </ul>
        </nav>
      </div>
    </div>
  </body>
</html>

